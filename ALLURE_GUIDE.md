# 📊 Структура отчётов Allure

## 🗂️ Папки с отчётами:

```
reports/
├── allure-results/       ← JSON файлы (сырые данные) - НЕ ОТКРЫВАТЬ!
│   ├── test1-result.json
│   ├── test2-result.json
│   └── ...100500 файлов
│
├── allure-report/        ← HTML отчёт (агрегированный) - ОТКРЫВАТЬ ЭТО!
│   ├── index.html        ← 🌐 Главная страница отчёта
│   ├── data/
│   ├── plugins/
│   └── ...
│
├── report.html           ← Обычный HTML отчёт с метриками API
└── api_coverage.json     ← JSON с метриками покрытия API
```

## ✅ Правильно:

```bash
# Открыть агрегированный HTML отчёт
open reports/allure-report/index.html

# Или запустить интерактивный сервер
allure open reports/allure-report
```

## ❌ Неправильно:

```bash
# НЕ открывайте эту папку! Здесь только JSON файлы
open reports/allure-results/
```

---

## 🚀 После запуска тестов:

```bash
python -m pytest
```

**Отчёт автоматически:**
1. ✅ Генерируется в `reports/allure-report/`
2. ✅ Открывается в браузере
3. ✅ Показывается путь в консоли

---

## 📊 Что содержит агрегированный отчёт:

- **Overview** - общая статистика (passed/failed/skipped)
- **Categories** - группировка по типам ошибок
- **Suites** - тесты по файлам
- **Graphs** - графики и диаграммы
- **Timeline** - временная линия выполнения
- **Behaviors** - группировка по фичам
- **Packages** - структура по пакетам

---

## 💡 Полезные команды:

```bash
# Открыть статический HTML
open reports/allure-report/index.html

# Запустить с live-reload
allure serve reports/allure-results

# Очистить старые отчёты
rm -rf reports/allure-results/* reports/allure-report/*
```

---

## 🔍 Если отчёт не сгенерировался:

1. Проверьте установку Allure:
   ```bash
   allure --version
   ```

2. Проверьте наличие результатов:
   ```bash
   ls -la reports/allure-results/
   ```

3. Сгенерируйте вручную:
   ```bash
   allure generate reports/allure-results -o reports/allure-report --clean
   ```

4. Откройте:
   ```bash
   open reports/allure-report/index.html
   ```
