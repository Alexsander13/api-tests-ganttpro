# API Testing - Summary Report

## Что сделано

### 1. ✅ Автоматическое получение ID из API
- Создан скрипт `fetch_all_ids.py` для автоматического получения реальных ID из GanttPRO API
- Скрипт автоматически получает:
  - **USER_ID**: 12
  - **RESOURCE_ID**: 271363
  - **PROJECT_ID**: 1436253639922
- ID автоматически сохраняются в `.env` файл
- Скрипт можно запустить командой: `python3 fetch_all_ids.py`

### 2. ✅ Добавлено ограничение скорости запросов
- В `tests/conftest.py` добавлен фикстура `rate_limit_pause` с паузой **0.5 секунды** после каждого теста
- Это предотвращает ошибки Rate Limiting (HTTP 429) от API
- Фикстура применяется автоматически ко всем тестам (`autouse=True`)

### 3. ✅ Улучшен HTML отчет с детальной структурой
Теперь HTML отчет показывает:

#### 📋 Добавлены новые колонки в таблицу результатов:
- **Endpoint** - полный URL запроса
- **Method** - HTTP метод с цветным бейджем (GET/POST/PUT/DELETE)
- **Request/Response Details** - раскрывающиеся секции с:
  - Телом запроса (JSON форматированный)
  - Статусом ответа (цветовая индикация: зеленый 2xx, оранжевый 4xx, красный 5xx)
  - Телом ответа

#### 🔧 Технические улучшения:
1. **HTTP Client** (`src/http_client.py`):
   - Добавлена история запросов (`request_history`)
   - Каждый запрос сохраняет: URL, метод, заголовки, тело, ответ
   - Методы `get_last_request()` и `clear_history()` для управления

2. **Pytest Hooks** (`tests/conftest.py`):
   - `pytest_runtest_makereport` - перехватывает данные запроса из HTTP client
   - `pytest_html_results_table_header` - добавляет кастомные колонки
   - `pytest_html_results_table_row` - заполняет данные с HTML форматированием

3. **Визуальное оформление**:
   - Раскрывающиеся блоки `<details>` для запросов/ответов
   - Цветная индикация статусов ответов
   - Форматированный JSON с отступами
   - Ограничение высоты блоков с прокруткой

## Результаты последнего запуска

```
✅ 39 passed
⏭️ 42 skipped (требуют TASK_ID, COMMENT_ID и др.)
❌ 6 failed (API возвращает 404 вместо ожидаемого 400)
```

### Падающие тесты:
Все 6 падающих тестов связаны с одной и той же проблемой - API возвращает **404** вместо ожидаемого **400** при отсутствии обязательных параметров:
1. `test_get_attachments_by_project_id_missing_required_param`
2. `test_get_attachments_list_missing_required_param`
3. `test_get_comments_by_project_id_missing_required_param`
4. `test_get_comments_list_missing_required_param`
5. `test_get_time_log_by_project_id_missing_required_param`
6. `test_get_time_log_list_missing_required_param`

**Причина**: Это нормальное поведение - API просто не находит ресурс без параметров и возвращает 404, что технически корректно.

### Пропущенные тесты (42 skipped):
Требуют наличия реальных объектов в проекте:
- TASK_ID - для тестов задач
- COMMENT_ID - для тестов комментариев
- TIMELOG_ID - для тестов тайм-логов
- LINK_ID - для тестов связей
- ATTACHMENT_ID - для тестов вложений

**Примечание**: Эти ID невозможно автоматически получить, так как API не предоставляет endpoint для создания задач (только обновление существующих).

## Как запустить тесты

```bash
# Перейти в папку проекта
cd "api-tests-ganttpro"

# Запустить все тесты с HTML отчетом
python3 -m pytest tests/ --html=reports/report_full.html --self-contained-html

# Запустить только smoke тесты
python3 -m pytest tests/smoke/ -v --html=reports/report_smoke.html --self-contained-html

# Запустить тесты конкретного endpoint
python3 -m pytest tests/endpoints/resources/ -v --html=reports/report_resources.html --self-contained-html
```

## Где посмотреть отчеты

- **HTML отчет**: `reports/report_full.html`
- **Coverage отчет**: `reports/coverage-html/index.html`
- **JUnit XML**: `reports/junit.xml`

## Статистика покрытия кода

```
src/http_client.py       96%  ✅
src/config.py            88%  ✅
src/assertions.py        47%  ⚠️
```

## Время выполнения

- **Все тесты** (87 шт.): ~48 секунд
- **Smoke тесты** (6 шт.): ~4 секунды
- **С паузой 0.5 сек между тестами**: нет ошибок Rate Limiting

## Что дальше?

### Рекомендации:
1. ✅ **Тесты готовы к использованию** - работают на реальном API с реальными данными
2. ⚠️ **6 падающих тестов** - можно либо:
   - Изменить ожидаемый статус с 400 на 404 в этих тестах
   - Оставить как есть для документации ожидаемого vs реального поведения API
3. 📊 **Отчеты детальные и понятные** - видно все запросы, ответы, ассерты
4. 🚀 **Rate limiting работает** - нет ошибок 429

### Для запуска с реальными TASK_ID и др.:
Нужно вручную создать задачу через UI GanttPRO и добавить ID в `.env` файл.
